/**
 *  Creating Arrays from Strings
 */

 var sentence = "Arrays can be created as the result of calling the split() function on a string";
 const words = sentence.split(" ");
 words.forEach(element => {
     console.log(words);
 });